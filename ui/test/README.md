# Test Vue UI

Start dependencies:

```bash
docker compose up -d
```

Run Vue app locally in development mode:

```bash
cd ..
npm run serve
```

Tear down dependencies:

```bash
docker compose down -v
```
