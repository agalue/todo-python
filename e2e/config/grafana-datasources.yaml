---
apiVersion: 1

datasources:

- name: Prometheus
  type: prometheus
  isDefault: true
  editable: true
  access: proxy
  url: http://prometheus:9090

- name: Mimir
  type: prometheus
  isDefault: false
  editable: true
  access: proxy
  url: http://mimir:9009/prometheus

- name: Loki
  type: loki
  isDefault: false
  editable: true
  access: proxy
  url: http://loki:3100
  jsonData:
    derivedFields:
    - name: TraceID
      datasourceUid: tempo
      matcherRegex: attributes_otelTraceID=(\w+)
      url: $${__value.raw}

- name: Tempo
  type: tempo
  isDefault: false
  editable: true
  access: proxy
  url: http://tempo:3200
